{
  "name": "",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "deploy:asd-interfejs": "npm run build && wrangler deploy --name asd-interfejs",
    "deploy:ddd-interfejs": "npm run build && wrangler deploy --name ddd-interfejs",
    "preview": "astro preview",
    "astro": "astro",
    "generate-types": "wrangler types"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^13.6.0",
    "astro": "^6.4.2",
    "wrangler": "^4.95.0"
  },
  "overrides": {
    "vite": "^7"
  }
}
