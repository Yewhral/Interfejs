<section id="about" class="section about" aria-labelledby="about-title">
	<div class="section-inner text-section">
		<p class="eyebrow">O uslugach</p>
		<h2 id="about-title">Lorem ipsum dolor sit.</h2>
		<p>
			Lorem ipsum dolor sit amet, consectetur adipiscing elit. Integer interfejs
			tworzy proste, uzyteczne strony, narzedzia i systemy, ktore pomagaja firmom
			porzadkowac procesy, szybciej testowac pomysly i lepiej komunikowac wartosc
			uslug. Vivamus luctus, justo at facilisis gravida, arcu sem porttitor nisl,
			nec posuere mi sapien id lectus.
		</p>
	</div>
</section>

<style>
	.section {
		padding: clamp(56px, 9vw, 112px) 0;
	}

	.section-inner {
		width: min(1120px, calc(100% - 40px));
		margin: 0 auto;
	}

	.about {
		border-top: 1px solid #e5e7eb;
		border-bottom: 1px solid #e5e7eb;
		background: #f8fafc;
	}

	.eyebrow {
		margin: 0 0 16px;
		color: var(--accent);
		font-size: 13px;
		font-weight: 800;
		letter-spacing: 0.12em;
		text-transform: uppercase;
	}

	h2 {
		max-width: 780px;
		margin: 0;
		color: #111827;
		font-size: clamp(34px, 5vw, 64px);
		line-height: 1;
		letter-spacing: 0;
	}

	.text-section p:last-child {
		max-width: 820px;
		margin: 28px 0 0;
		color: #334155;
		font-size: clamp(18px, 2vw, 22px);
		line-height: 1.7;
	}

	@media (max-width: 520px) {
		.section {
			padding: 48px 0;
		}

		.section-inner {
			width: min(100% - 28px, 1120px);
		}
	}
</style>
