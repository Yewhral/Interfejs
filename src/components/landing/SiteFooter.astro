<footer id="contact" class="site-footer">
	<div class="footer-inner">
		<p>interfejs</p>
		<a href="mailto:hello@interfejs.local">hello@interfejs.local</a>
	</div>
</footer>

<style>
	.site-footer {
		border-top: 1px solid #e5e7eb;
		background: #111827;
		color: #ffffff;
	}

	.footer-inner {
		display: flex;
		width: min(1120px, calc(100% - 40px));
		align-items: center;
		justify-content: space-between;
		gap: 20px;
		margin: 0 auto;
		padding: 28px 0;
	}

	p {
		margin: 0;
		font-weight: 800;
	}

	a {
		border-radius: 6px;
		color: #e5e7eb;
		font-weight: 700;
		text-decoration: none;
	}

	a:hover {
		color: #ffffff;
	}

	a:focus-visible {
		outline: 3px solid color-mix(in srgb, var(--accent) 45%, #ffffff);
		outline-offset: 3px;
	}

	@media (max-width: 520px) {
		.footer-inner {
			width: min(100% - 28px, 1120px);
			align-items: flex-start;
			flex-direction: column;
		}
	}
</style>
